export enum AccountType {
   STUDENT = "STUDENT",
   EMPLOYER = "EMPLOYER",
}

export enum AccountStatus {
   ACTIVE = "ACTIVE",
   DEACTIVE = "DEACTIVE",
   APPROVAL_PENDING = "APPROVAL_PENDING",
   BLOCKED = "BLOCKED",
   DELETED = "DELETED",
   INACTIVE = "INACTIVE",
}